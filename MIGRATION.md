# คู่มือย้ายฐานข้อมูล MySQL → PostgreSQL (สำหรับระบบที่ใช้งานจริงแล้ว)

ระบบ TD ServicePro เปลี่ยนฐานข้อมูลหลักจาก MySQL มาเป็น PostgreSQL คู่มือนี้สำหรับ
**deploy ที่มีข้อมูลลูกค้า/ตั๋วซ่อม/ยอดขายจริงอยู่แล้ว** และต้องการย้ายข้อมูลเดิมมาด้วย
(ถ้าเป็นการติดตั้งใหม่ที่ยังไม่มีข้อมูล ไม่ต้องอ่านหน้านี้ ทำตาม `DOCKER.md` วิธีที่ 1 ได้เลย)

**หลักการสำคัญ:** ตลอดขั้นตอนนี้ ฐานข้อมูล MySQL เดิมจะยังไม่ถูกแตะต้อง/ลบทิ้งจนกว่าจะยืนยันแล้วว่า
ข้อมูลใน PostgreSQL ครบถ้วนถูกต้อง 100% — ถ้าขั้นตอนไหนพลาด ระบบเดิม (MySQL) ยังใช้งานได้ตามปกติ
เสมอ ไม่มีจุดไหนที่ "จุดที่ย้อนกลับไม่ได้" จนกว่าจะถึงขั้นตอนสุดท้ายที่คุณกดยืนยันเอง

---

## ภาพรวมขั้นตอน

1. สำรองข้อมูล MySQL เดิม
2. ตั้ง container PostgreSQL ใหม่ขึ้นมา **คู่ขนาน** กับ MySQL เดิม (ยังไม่ปิด MySQL)
3. รันสคริปต์ `migrate_mysql_to_postgres.py` เพื่อคัดลอกข้อมูลจริงทั้งหมดจาก MySQL → PostgreSQL
4. ตรวจสอบจำนวนแถว + ทดสอบเข้าระบบจริงผ่าน PostgreSQL
5. ตัดมาใช้ PostgreSQL จริง (เปลี่ยน `docker-compose.yml` ให้แอปหลักชี้ไปที่ PostgreSQL)
6. เก็บ MySQL เดิมไว้สำรอง (อย่างน้อย 1-2 สัปดาห์) ก่อนค่อยลบทิ้ง

---

## ขั้นตอนที่ 1 — สำรองข้อมูล MySQL เดิม

```bash
docker exec tdprinter-mysql mysqldump -u root -p<รหัสผ่าน root ของ MySQL เดิม> tdprinter_care \
  > backup_mysql_before_postgres_migration_$(date +%Y%m%d).sql
```

เก็บไฟล์นี้ไว้ในที่ปลอดภัย (นอกเครื่อง server ด้วยยิ่งดี) — นี่คือตาข่ายนิรภัยสุดท้ายถ้าทุกอย่างผิดพลาด

## ขั้นตอนที่ 2 — เตรียม PostgreSQL คู่ขนานกับ MySQL เดิม

ไฟล์ `docker-compose.yml` ที่แนบมาให้เวอร์ชันนี้ **แทนที่** service `mysql` เดิมด้วย `postgres` แล้ว
(ไม่ได้รันคู่กันในไฟล์เดียวกัน) วิธีที่ปลอดภัยที่สุดคือแยกสองขั้นตอน:

1. **อย่าเพิ่งเอาไฟล์ `docker-compose.yml` ใหม่นี้ไปทับของเดิมที่กำลังรันอยู่** — MySQL container
   (`tdprinter-mysql`) เดิมต้องรันต่อไปตามปกติในระหว่างนี้ทั้งหมด
2. ตั้งค่า `.env` ใหม่ (คัดลอกจาก `.env.example` เวอร์ชันนี้) ใส่รหัสผ่าน `PG_PASSWORD` ที่ต้องการ
3. สั่งสร้างเฉพาะ container `postgres` ขึ้นมาก่อน (ยังไม่ต้องรัน `tdprinter-care` ตัวใหม่):
   ```bash
   docker compose up -d postgres
   ```
4. รอจน container postgres พร้อม (เช็คด้วย `docker compose ps` ต้องขึ้น `healthy`)
5. สร้างตาราง (schema) เปล่าๆ ใน PostgreSQL ให้พร้อมรับข้อมูล โดยรันแอปตัวใหม่ครั้งเดียวแบบ
   ปิดการ seed ข้อมูลตัวอย่างไว้ก่อน:
   ```bash
   docker compose run --rm -e SEED_DEMO_DATA=0 tdprinter-care python app.py &
   sleep 5 && docker compose stop tdprinter-care
   ```
   (ขั้นตอนนี้แค่ให้ `db.init_db()` สร้างตาราง/CHECK constraint ให้ครบ ไม่ต้องปล่อยให้รันค้างไว้ —
   สคริปต์ `migrate_mysql_to_postgres.py` ในขั้นตอนถัดไปจะเรียก `db.init_db()` ซ้ำให้เองอีกทีด้วย
   จึงข้ามขั้นตอนนี้ไปเลยก็ได้ ถ้าไม่แน่ใจให้ทำไว้ก่อนเพื่อเช็คว่า container/env ถูกต้องจริง)

## ขั้นตอนที่ 3 — รันสคริปต์ย้ายข้อมูล

สคริปต์ `migrate_mysql_to_postgres.py` (อยู่ในโฟลเดอร์เดียวกับ `app.py`) จะ:
- เชื่อมต่อทั้ง MySQL เดิมและ PostgreSQL ใหม่พร้อมกัน
- สร้างตาราง PostgreSQL ให้ครบถ้วนถ้ายังไม่มี (เรียก `db.init_db()` ให้เอง โดยปิดการ seed
  ข้อมูลตัวอย่างชั่วคราวระหว่างรันสคริปต์)
- คัดลอกข้อมูลทุกตาราง (22 ตาราง) ตามลำดับที่ปลอดภัยต่อ foreign key โดย **คงค่า primary key เดิม
  ทุกแถว** (ไม่ให้ระบบสร้างเลขใหม่) เพื่อรักษาความสัมพันธ์ระหว่างตารางทั้งหมดให้ตรงกับของเดิม
- รีเซ็ต sequence ของ PostgreSQL ให้ตรงกับข้อมูลที่เพิ่งนำเข้า (กันปัญหาเลขที่ซ้ำตอนสร้างแถวใหม่ในอนาคต)
- สรุปจำนวนแถวเทียบ MySQL ↔ PostgreSQL ทุกตารางให้ตรวจสอบท้ายรายงาน

**ต้องเชื่อมต่อ MySQL เดิมและ PostgreSQL ใหม่จากเครื่องเดียวกันได้พร้อมกัน** — ถ้ารันจากเครื่อง
host (นอกคอนเทนเนอร์) ให้เปิดพอร์ตออกมาที่ `127.0.0.1` ชั่วคราว (เอาออกทีหลังหลัง migration
เสร็จ เพื่อความปลอดภัย): เพิ่ม `ports: ["3306:3306"]` ใน container MySQL เดิม และ
`ports: ["5432:5432"]` ใน container `postgres` ใหม่ แล้ว restart ทั้งสอง container

ติดตั้งไลบรารีที่สคริปต์ต้องใช้ (คนละชุดกับที่ตัวแอปใช้ เพราะสคริปต์นี้ต้องคุยกับ MySQL ด้วย):
```bash
pip install pymysql psycopg2-binary
```

รันสคริปต์:
```bash
SRC_MYSQL_HOST=127.0.0.1 SRC_MYSQL_USER=tdprinter SRC_MYSQL_PASSWORD=<รหัสผ่าน MySQL เดิม> \
SRC_MYSQL_DATABASE=tdprinter_care \
PG_HOST=127.0.0.1 PG_USER=tdprinter PG_PASSWORD=<รหัสผ่าน PostgreSQL ใหม่> \
PG_DATABASE=tdprinter_care \
python3 migrate_mysql_to_postgres.py
```

สคริปต์จะถามยืนยัน (พิมพ์ `MIGRATE`) ก่อนเขียนข้อมูลจริงเสมอ และจะ **ปฏิเสธไม่ทำงาน** ถ้าฐานข้อมูล
PostgreSQL ปลายทางมีข้อมูลอยู่แล้ว (กันรันซ้ำแล้วข้อมูลซ้ำซ้อน) — ถ้าต้องรันซ้ำจริงๆ (เช่นแก้ปัญหา
แล้วอยากเริ่มใหม่) ให้ลบข้อมูลใน PostgreSQL ให้ว่างก่อน (หรือลบ+สร้าง container postgres ใหม่ทั้งลูก
เพื่อความชัวร์) แล้วค่อยรันสคริปต์ใหม่

## ขั้นตอนที่ 4 — ตรวจสอบก่อนตัดเข้าใช้งานจริง

1. ดูรายงานท้ายสคริปต์ — **จำนวนแถวของทุกตารางต้องตรงกันระหว่าง MySQL กับ PostgreSQL** ถ้ามีตาราง
   ไหนไม่ตรง สคริปต์จะแจ้งเตือนชัดเจนและจบด้วย exit code ที่ไม่ใช่ 0 — ห้ามใช้งานต่อจนกว่าจะหาสาเหตุ
   เจอ (สาเหตุที่เป็นไปได้: การเชื่อมต่อ MySQL หลุดกลางคัน, ตารางถูกแก้ข้อมูลระหว่างที่สคริปต์กำลังรัน)
2. รันแอปตัวใหม่ชี้ไปที่ PostgreSQL (ยังไม่ต้องเปลี่ยน DNS/reverse proxy จริง แค่ทดสอบผ่านพอร์ต
   ชั่วคราวหรือ `docker compose exec` ก่อน) แล้วตรวจสอบอย่างน้อย:
   - ล็อกอินได้ทุกสิทธิ์ (admin/manager/technician/sales/customer) ด้วยบัญชีเดิมที่มีอยู่จริง
   - เปิดตั๋วซ่อมเก่าดูรายละเอียด/ประวัติ/รูปภาพที่แนบได้ครบ
   - ดูหน้ารายงาน (Reports) เห็นข้อมูลย้อนหลังถูกต้อง
   - ดูสต็อกสินค้า/ยอดขายที่ผ่านมาถูกต้อง
   - ทดสอบสร้างตั๋วซ่อม/ใบเสนอราคาใหม่ได้ตามปกติ (เช็คว่าเลขที่ใหม่ไม่ชนกับของเดิม — ถ้า sequence
     ถูกรีเซ็ตถูกต้อง เลขที่ใหม่จะต่อจากเลขสูงสุดเดิมเสมอ)

## ขั้นตอนที่ 5 — ตัดมาใช้งานจริง

เมื่อมั่นใจแล้วว่าข้อมูลครบถ้วนถูกต้อง:

1. หยุด container แอปตัวเก่าที่ยังชี้ไป MySQL (`docker compose down` บนไฟล์ compose เดิม หรือ
   `docker stop tdprinter-care`)
2. เอาไฟล์ `docker-compose.yml` / `.env.example` / `requirements.txt` / `Dockerfile` ชุดใหม่
   (เวอร์ชัน PostgreSQL) มาใช้แทนของเดิมทั้งหมด — ตั้งค่า `.env` ให้ครบ (`PG_PASSWORD` ฯลฯ)
3. `docker compose up -d --build` — ครั้งนี้แอปจะต่อ PostgreSQL ที่มีข้อมูลครบแล้วจากขั้นตอนที่ 3
4. ปิดพอร์ตชั่วคราวที่เปิดไว้ตอนขั้นตอนที่ 3 (ถ้าเปิดไว้) ทั้งฝั่ง MySQL และ PostgreSQL กลับคืน
5. ทดสอบเข้าเว็บจริงผ่านโดเมนจริงอีกครั้ง

## ขั้นตอนที่ 6 — เก็บ MySQL เดิมไว้สำรอง

**อย่าเพิ่งลบ container/โฟลเดอร์ `./mysql-data` ของ MySQL เดิมทันที** — แนะนำเก็บไว้อย่างน้อย
1-2 สัปดาห์ (หรือจนกว่าจะมั่นใจว่าใช้งานจริงบน PostgreSQL ได้เรียบร้อยดีทุกฟีเจอร์) โดยหยุด
container MySQL ไว้เฉยๆ ก็พอ (`docker stop tdprinter-mysql` ไม่ต้อง `docker rm`) ไม่กินทรัพยากร
เมื่อไม่ได้รัน แล้วค่อยลบทิ้งทีหลังเมื่อมั่นใจแล้วจริงๆ

---

## แผนย้อนกลับ (Rollback) ถ้ามีปัญหาระหว่างทาง

เนื่องจากขั้นตอนที่ 1-4 ไม่ได้แตะต้อง MySQL เดิมเลย (แค่อ่านข้อมูลออกมา ไม่มีการเขียน/ลบ) การย้อนกลับ
ก่อนถึงขั้นตอนที่ 5 ทำได้ง่ายมาก — แค่ไม่ต้องทำขั้นตอนที่ 5 ต่อ ระบบเดิมบน MySQL ยังรันอยู่ตามปกติ
ไม่มีอะไรเสียหาย

ถ้าตัดมาใช้ PostgreSQL แล้ว (ทำขั้นตอนที่ 5 ไปแล้ว) แล้วพบปัญหาร้ายแรงภายหลัง สามารถย้อนกลับไปใช้
MySQL เดิมชั่วคราวได้โดย:
1. `docker compose down` (ปิดแอปตัวที่ต่อ PostgreSQL)
2. เอาไฟล์ `docker-compose.yml`/`.env`/`requirements.txt`/`Dockerfile` เวอร์ชัน MySQL เดิมกลับมาใช้
3. `docker compose up -d --build` (จะต่อ MySQL container ที่ยังเก็บไว้ในขั้นตอนที่ 6)

ข้อควรระวัง: ถ้ามีการสร้างตั๋วซ่อม/ข้อมูลใหม่ๆ บน PostgreSQL หลังตัดมาใช้งานจริงแล้ว การย้อนกลับไป
MySQL แบบนี้จะ **ไม่มี** ข้อมูลใหม่เหล่านั้น (เพราะสร้างอยู่ฝั่ง PostgreSQL เท่านั้น) — จึงควรมั่นใจใน
ขั้นตอนที่ 4 ให้มากที่สุดก่อนตัดสินใจเข้าขั้นตอนที่ 5 เพื่อลดโอกาสต้องย้อนกลับ
